/*
Resource management
*/

process {
    errorStrategy='retry'
    maxRetries=2

    // basic resources
    cpus = { 1 * task.attempt }
    time = { 1.h * task.attempt }
    memory = { 4.GB * task.attempt }
    
}

timeline {
    enabled = true
}
